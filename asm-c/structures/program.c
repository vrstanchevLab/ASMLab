#include <stdio.h>
#include "program.h"
int main(void){
struct person p={.name="Veselin Stanchev",.age=25,.g='M'};
struct house h={.hnum=1,.rooms=11,.color="white"};
void introduceP(struct person p);
void introduceH(struct house h);

return (0);
}
